import { cn } from '@/lib/utils'
export function Badge({children,className,tone='neutral'}:{children:React.ReactNode,className?:string,tone?:'neutral'|'blue'|'coral'|'green'|'citron'}) { return <span className={cn('inline-flex min-h-7 items-center rounded-full border px-2.5 text-xs font-semibold tracking-[.02em]', tone==='neutral'&&'border-rule bg-sheet',tone==='blue'&&'border-pencil/20 bg-pencil/8 text-pencil',tone==='coral'&&'border-coral/25 bg-coral/8 text-[#a93f32]',tone==='green'&&'border-leaf/25 bg-leaf/8 text-leaf',tone==='citron'&&'border-carbon/10 bg-citron',className)}>{children}</span> }
